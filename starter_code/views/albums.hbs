<div class="container">
    {{#each albumsList}}
    <div class="item-wrapper">
        <img src="{{images.0.url}}" style="height: 200px;">
        <p>{{name}}</p>
        <a href="#">View Tracks</a>
    </div>
    {{/each}}
</div>